import WebComponent

defmodule Component do
    def init() do 
        state = 1 + 3

        WebComponent.init(
            "custom-element",
            Component.template, 
            state
        )
    end

    def template(model) do
        "hello world! ${model.name}"
    end
end
